{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Run",
            "sourceLanguages": [
                "rust"
            ],
            "terminal": "integrated",
            "preLaunchTask": "cargo run",
            "program": "${workspaceRoot}/target/debug/rpn",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}